# dosfstools -- FAT utilities (mkfs.fat, fsck.fat).
#
# Supersedes Stage2/recipes/dosfstools (release 2): --sbindir=/usr/sbin wrote
# through the merged-/usr symlink. fsck.fat in particular has to be on
# mkinitcpio's forced PATH=/usr/bin:/bin for the ESP to be checked at boot.

schema_version = 1

[package]
name = "dosfstools"
version = "4.2"
release = "3"
description = "DOS FAT filesystem utilities (mkfs.fat, fsck.fat)"
license = "GPL-3.0-or-later"
channel = "system"

[source]
url = "https://github.com/dosfstools/dosfstools/releases/download/v4.2/dosfstools-4.2.tar.gz"
sha256 = "64926eebf90092dca21b14259a5301b7b98e7b1943e8a201c7d726084809b527"

dependencies = [
    "glibc >= 2.44"
]

provides = [
    "dosfstools"
]

prepare = []

build = [
    "./configure --prefix=/usr --sbindir=/usr/bin --enable-compat-symlinks",
    "make -j$(nproc)"
]

install = [
    "make install DESTDIR=$DESTDIR"
]
