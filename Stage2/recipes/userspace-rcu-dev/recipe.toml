schema_version = 1

[package]
name = "userspace-rcu-dev"
version = "0.15.6"
release = "2"
description = "Userspace RCU headers, pkg-config and linker symlink"
license = "LGPL-2.1-or-later"
channel = "system"

[source]
url = "https://lttng.org/files/urcu/userspace-rcu-0.15.6.tar.bz2"
sha256 = "850b192096eb11ebf2c70e8f97bc7da7479ee41da1bebeb44e3986908bac414f"

dependencies = [
    "userspace-rcu >= 0.15.6"
]

provides = [
    "userspace-rcu-dev"
]

prepare = []

build = [
    "./configure --prefix=/usr --disable-static",
    "make -j$(nproc)"
]

install = [
    "make install DESTDIR=$DESTDIR",
    "rm -rf $DESTDIR/usr/bin $DESTDIR/usr/sbin $DESTDIR/usr/share $DESTDIR/etc $DESTDIR/var 2>/dev/null || true",
    "find $DESTDIR/usr/lib $DESTDIR/usr/lib64 -type f -name '*.so.*' -delete 2>/dev/null || true"
]
