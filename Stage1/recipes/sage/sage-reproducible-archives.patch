diff --git a/src/cli/main.cpp b/src/cli/main.cpp
--- a/src/cli/main.cpp
+++ b/src/cli/main.cpp
@@ -156,7 +156,15 @@ int cmd_build(const CliOptions& opts) {
     manifest.arch = r.arch;
 
     if (std::filesystem::exists(pkg_dir)) {
-        for (const auto& entry : std::filesystem::recursive_directory_iterator(pkg_dir, std::filesystem::directory_options::skip_permission_denied)) {
+        std::vector<std::filesystem::directory_entry> entries;
+        for (const auto& entry : std::filesystem::recursive_directory_iterator(pkg_dir, std::filesystem::directory_options::skip_permission_denied)) {
+            entries.push_back(entry);
+        }
+        std::ranges::sort(entries, {}, [&pkg_dir](const auto& entry) {
+            return entry.path().lexically_relative(pkg_dir).generic_string();
+        });
+
+        for (const auto& entry : entries) {
             if (entry.is_symlink()) continue;
             if (entry.is_regular_file()) {
                 auto elf_res = sage::util::scan_elf(entry.path());
diff --git a/src/core/sage.archive.cppm b/src/core/sage.archive.cppm
--- a/src/core/sage.archive.cppm
+++ b/src/core/sage.archive.cppm
@@ -393,7 +393,15 @@ inline std::expected<void, std::string> create_package(
 
     // 3. Append data/... filesystem payload
     if (std::filesystem::exists(data_dir)) {
-        for (const auto& entry : std::filesystem::recursive_directory_iterator(data_dir, std::filesystem::directory_options::none)) {
+        std::vector<std::filesystem::directory_entry> entries;
+        for (const auto& entry : std::filesystem::recursive_directory_iterator(data_dir, std::filesystem::directory_options::none)) {
+            entries.push_back(entry);
+        }
+        std::ranges::sort(entries, {}, [&data_dir](const auto& entry) {
+            return entry.path().lexically_relative(data_dir).generic_string();
+        });
+
+        for (const auto& entry : entries) {
             auto rel = entry.path().lexically_relative(data_dir).generic_string();
             std::string tar_name = "data/" + rel;
 
