schema_version = 1

[package]
name = "iana-etc"
version = "20260811"
release = "1"
description = "IANA protocol and service data (/etc/protocols, /etc/services)"
license = "MIT"
channel = "system"

[source]
url = "https://github.com/Mic92/iana-etc/releases/download/20260811/iana-etc-20260811.tar.gz"
sha256 = ""

dependencies = [
]

build_dependencies = [
]

provides = [
    "iana-etc",
]

prepare = [
]

build = [
]

install = [
    "mkdir -p $DESTDIR/etc",
    "cp -v protocols services $DESTDIR/etc/",
]
