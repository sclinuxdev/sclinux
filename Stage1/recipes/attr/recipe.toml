schema_version = 1

[package]
name = "attr"
version = "2.6.0"
release = "1"
description = "Extended attribute support tools and library"
license = "GPL-2.0-or-later"
channel = "system"

[source]
url = "https://download-mirror.savannah.gnu.org/releases/attr/attr-2.6.0.tar.xz"
sha256 = ""

dependencies = [
]

provides = [
    "attr",
    "so:libattr.so.1",
]

prepare = [
]

build = [
    "./configure --prefix=/usr --disable-static --sysconfdir=/etc --docdir=/usr/share/doc/attr-2.6.0",
    "make -j$(nproc)",
]

install = [
    "make install DESTDIR=$DESTDIR",
]
